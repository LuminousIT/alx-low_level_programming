#include "main.h"
#include <stdlib.h>

/**
 * _atoi - Entry Point
 *
 * Description: 'conv str to int'
 * @s: accepts *s char
 * Return: Always 0 (Success)
 */
int _atoi(char *s)
{
	return (atoi(s));
}
