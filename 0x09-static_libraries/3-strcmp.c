#include "main.h"
#include "string.h"

/**
 * _strcmp - Entry Point
 *
 * Description: function that compares two strings.
 * @s1: initial str.
 * @s2: sec str
 * Return: int (result)
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
